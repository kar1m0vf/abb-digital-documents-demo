import { accounts, periods, operations } from '../data.js';
import { state, selectedAccounts, accountDetail } from '../store.js';
import { actions, heading, notice, accountRow, accountVisual, languageChoices, errorHtml } from '../components.js';
import { icon, escapeHtml as esc, today } from '../utils.js';
export function accountSelection(){return `${heading('Məhsulu seçin','Sənədə daxil etmək istədiyiniz hesab və kartları seçin.',true)}${notice('Kredit kartlarının balansı mənfi görünə bilər. Kredit kartından çıxarış əldə etmək üçün filiala yaxınlaşın.')}<form id="accounts-form"><div class="products-column">${['card','account'].map(type=>`<section class="panel"><div class="product-toolbar"><h3>${type==='card'?'Kartlar':'Hesablar'}</h3><button type="button" class="text-link" data-action="balances">${icon(state.showBalances?'eye-off':'eye')} Balansı ${state.showBalances?'gizlət':'göstər'}</button></div><div class="account-list">${accounts.filter(a=>a.type===type).map(a=>accountRow(a,{checked:state.draft.accounts.includes(a.id),show:state.showBalances})).join('')}</div></section>`).join('')}<p class="selection-summary" aria-live="polite" id="selection-summary">${state.draft.accounts.length} məhsul seçilib</p></div>${errorHtml()}${actions('next','İrəli',!state.draft.accounts.length)}</form>`;}
const chips=(name,choices,value)=>`<div class="choice-row">${Object.entries(choices).map(([id,label])=>`<label class="chip"><input class="sr-only" type="radio" name="${name}" value="${id}" ${String(value)===id?'checked':''}>${label}</label>`).join('')}</div>`;
export function details(){return `${heading(state.draft.type==='statement'?'Çıxarış detalları':'Arayış detalları','Seçdiyiniz hər hesab və kart üçün parametrləri ayrıca tənzimləyin.')}<form id="details-form"><div class="products-column">${selectedAccounts().map(a=>{const d=accountDetail(a.id);return `<section class="panel detail-card" data-account-id="${a.id}"><div class="account-heading">${accountVisual(a)}<div><h3>${a.name}</h3><p class="muted">${a.currency} ···· ${a.last4}</p></div></div><label class="toggle-row"><span><strong>Ekvivalent valyuta</strong><p>Seçildikdə, məbləğ arayışda əlavə valyuta ilə əks olunacaq.</p></span><span class="switch"><input type="checkbox" name="equivalent-${a.id}" data-detail="equivalent" role="switch" aria-label="${a.name} ${a.last4}: ekvivalent valyuta" ${d.equivalent?'checked':''}></span></label><div class="equivalent-fields field" ${d.equivalent?'':'hidden'}><label for="currency-${a.id}">Əlavə valyuta</label><select id="currency-${a.id}" data-detail="equivalentCurrency">${['EUR','USD','AZN'].filter(c=>c!==a.currency).map(c=>`<option value="${c}" ${d.equivalentCurrency===c?'selected':''}>${c}</option>`).join('')}</select></div><fieldset class="fieldset field-group"><legend>Sənədin dili</legend>${languageChoices(`detail-language-${a.id}`,d.language)}</fieldset>${state.draft.type==='statement'?`<fieldset class="fieldset field-group"><legend>Müddət</legend>${chips(`period-${a.id}`,periods,d.period)}</fieldset><div class="two-col field-group custom-dates" ${d.period==='custom'?'':'hidden'}><div class="field"><label for="start-${a.id}">Başlanğıc tarixi</label><input id="start-${a.id}" type="date" data-detail="start" value="${esc(d.start)}" max="${today()}"></div><div class="field"><label for="end-${a.id}">Son tarix</label><input id="end-${a.id}" type="date" data-detail="end" value="${esc(d.end)}" max="${today()}"></div></div><fieldset class="fieldset field-group"><legend>Çıxarış növü</legend>${chips(`operation-${a.id}`,operations,d.operation)}</fieldset>`:''}</section>`;}).join('')}</div>${errorHtml()}${actions()}</form>`;}
