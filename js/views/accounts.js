import { accounts, periods, operations, MAX_SELECTED_ACCOUNTS } from '../data.js';
import { state, selectedAccounts, accountDetail } from '../store.js';
import { actions, heading, notice, accountRow, accountVisual, languageChoices, errorHtml } from '../components.js';
import { icon, escapeHtml as esc, today } from '../utils.js';
const currencies=[['RUB','details-imgEllipse26.png'],['USD','details-imgEllipse27.png'],['EUR','details-imgEllipse28.png'],['GBP','details-imgEllipse29.png']];
export function accountSelection(){return `${heading('Məhsulu seçin','',true)}${notice('Kredit kartlarının balansı mənfi görsənə bilər. Kredit kartlarından çıxarış əldə etmək üçün filiala yaxınlaşmağınız xahiş olunur','account-notice','')}<form id="accounts-form"><div class="products-column">${['card','account'].map(type=>`<section class="panel"><div class="product-toolbar"><h3>${type==='card'?'Kartlar':'Hesablar'}</h3><button type="button" class="balance-toggle" data-action="balances" aria-label="Balansı ${state.showBalances?'gizlət':'göstər'}" title="Balansı ${state.showBalances?'gizlət':'göstər'}">${icon(state.showBalances?'eye-off':'eye')}</button></div><div class="account-list">${accounts.filter(a=>a.type===type).map(a=>accountRow(a,{checked:state.draft.accounts.includes(a.id),show:state.showBalances,locked:state.draft.accounts.length>=MAX_SELECTED_ACCOUNTS})).join('')}</div></section>`).join('')}<p class="selection-summary" aria-live="polite" id="selection-summary">${state.draft.accounts.length}/${MAX_SELECTED_ACCOUNTS} məhsul seçilib</p></div>${errorHtml()}${actions('next','İrəli',!state.draft.accounts.length)}</form>`;}
const chips=(name,choices,value)=>`<div class="choice-row">${Object.entries(choices).map(([id,label])=>`<label class="chip"><input class="sr-only" type="radio" name="${name}" value="${id}" ${String(value)===id?'checked':''}>${label}</label>`).join('')}</div>`;
export function details(){return `${heading(state.draft.type==='statement'?'Çıxarış detalları':'Arayış detalları','Seçdiyiniz hər hesab/kart üçün parametrləri ayrıca tənzimləyin')}<form id="details-form"><div class="products-column">${selectedAccounts().map(a=>{const d=accountDetail(a.id);return `<section class="panel detail-card" data-account-id="${a.id}"><div class="account-heading">${accountVisual(a)}<div><h3>${a.name}</h3><p class="muted">${a.currency} ···· ${a.last4}</p></div></div><label class="toggle-row"><span><strong>Ekvivalent valyuta</strong><p>seçildikdə, məbləğ arayış blankında əlavə valyuta ilə əks olunacaq</p></span><span class="switch"><input type="checkbox" name="equivalent-${a.id}" data-detail="equivalent" role="switch" aria-label="${a.name} ${a.last4}: ekvivalent valyuta" ${d.equivalent?'checked':''}></span></label><fieldset class="equivalent-fields" ${d.equivalent?'':'hidden'}><legend class="sr-only">Əlavə valyuta</legend><div class="currency-options">${currencies.map(([currency,image])=>`<label class="currency-choice"><input class="sr-only" type="radio" name="currency-${a.id}" value="${currency}" data-detail="equivalentCurrency" ${d.equivalentCurrency===currency?'checked':''}><img src="./assets/figma/${image}" width="40" height="40" alt="">${currency}</label>`).join('')}</div></fieldset><fieldset class="fieldset field-group"><legend>Çıxarışın Dili</legend>${languageChoices(`detail-language-${a.id}`,d.language)}</fieldset>${state.draft.type==='statement'?`<fieldset class="fieldset field-group"><legend>Müddət</legend>${chips(`period-${a.id}`,Object.fromEntries(Object.entries(periods).filter(([id])=>id!=='custom')),d.period)}</fieldset><div class="two-col field-group custom-dates" ${d.period==='custom'?'':'hidden'}><div class="field"><label for="start-${a.id}">Başlanğıc tarixi</label><input id="start-${a.id}" type="date" data-detail="start" value="${esc(d.start)}" max="${today()}"></div><div class="field"><label for="end-${a.id}">Son tarix</label><input id="end-${a.id}" type="date" data-detail="end" value="${esc(d.end)}" max="${today()}"></div></div><fieldset class="fieldset field-group"><legend>Çıxarış növü</legend>${chips(`operation-${a.id}`,operations,d.operation)}</fieldset>`:''}</section>`;}).join('')}</div>${errorHtml()}${actions()}</form>`;}
